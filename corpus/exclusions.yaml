# Deferred-feature exclusion manifest.
# Generated by scripts/generate-corpus.rb from the Ruby plurimath gem. Do not edit.
# Provenance lives in the sidecar manifest beside this file.
---
schema: plurimath-corpus/exclusions/1
description: Cases withheld from every generator because their input matches a deferred construct (ARCHITECTURE.md §5).
features:
- unitsml
classes:
- Math::Function::Unitsml
excluded:
- id: text-unitsml-invalid
  group: quoted-text
  input: '"unitsml(zzz)"'
  input_format: asciimath
  feature: unitsml
  matched: unitsml\(
  reason: unitsml is deferred (ARCHITECTURE.md §5); matched on the input text, since the gem raises for invalid unitsml and produces no formula to inspect
- id: text-unitsml-valid
  group: quoted-text
  input: '"unitsml(kg)"'
  input_format: asciimath
  feature: unitsml
  matched: unitsml\(
  reason: unitsml is deferred (ARCHITECTURE.md §5); matched on the input text, since the gem raises for invalid unitsml and produces no formula to inspect
