# Deferred-feature exclusion manifest.
# Generated by scripts/generate-corpus.rb from the Ruby plurimath gem. Do not edit.
# Provenance lives in the sidecar manifest beside this file.
---
schema: plurimath-corpus/exclusions/1
description: Cases withheld from every generator because their input matches a deferred construct (ARCHITECTURE.md §5).
features:
- unitsml
classes:
- Math::Function::Unitsml
excluded:
- id: text-unitsml-invalid
  group: quoted-text
  input: '"unitsml(zzz)"'
  input_format: asciimath
  feature: unitsml
  matched: unitsml\(
  raises: true
  reason: unitsml is deferred (ARCHITECTURE.md §5). This input raises in the gem, so there is no formula to classify and the match is made on the input text
- id: text-unitsml-valid
  group: quoted-text
  input: '"unitsml(kg)"'
  input_format: asciimath
  feature: unitsml
  matched: unitsml\(
  raises: false
  reason: unitsml is deferred (ARCHITECTURE.md §5). This input parses in the gem; it is withheld because the feature is deferred, not because anything failed
