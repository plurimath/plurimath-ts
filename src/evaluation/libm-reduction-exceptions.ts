/**
 * GENERATED by `scripts/measure-libm-reduction-exhaustive.mjs --write`. Do not edit.
 *
 * The arguments `|x| < SMALL_ARGUMENT` (`libm.ts`), within `2^-35` of a
 * nonzero multiple of `pi/2`, where glibc's `sin`/`cos`/`tan` was measured
 * different from the correctly rounded double and the exact result lies
 * outside the function's band — the only ones the band alone would answer
 * wrongly. Stored as magnitudes; both signs are refused.
 */
export const REDUCTION_EXCEPTIONS: Readonly<Record<"sin" | "cos" | "tan", ReadonlySet<number>>> = {
  sin: new Set<number>([]),
  cos: new Set<number>([1.5707963267948968]),
  tan: new Set<number>([9.42477796076938, 18.84955592153876]),
};
